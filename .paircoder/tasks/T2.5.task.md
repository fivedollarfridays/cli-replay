---
id: T2.5
title: 'Packaging polish: py.typed, .gitignore, pyproject.toml, version, CI'
plan: plan-2026-03-plan-2026-03-clireplay-hardening
type: chore
priority: P2
complexity: 10
status: done
sprint: null
tags: []
depends_on: []
completed_at: '2026-03-04T03:15:25.478105'
---

# Objective

Add PEP 561 py.typed marker, fix .gitignore gaps, add pyproject.toml metadata, single-source version via importlib.metadata, fix CI lint job python version.

# Acceptance Criteria

- [x] `cli_replay/py.typed` exists (empty marker)
- [x] `pyproject.toml` has `[tool.setuptools.package-data]` for py.typed
- [x] `pyproject.toml` has classifiers and urls
- [x] `__init__.py` uses `importlib.metadata.version()` instead of hardcoded string
- [x] `.gitignore` includes .mypy_cache/, .pytest_cache/, .ruff_cache/, htmlcov/
- [x] CI lint job uses Python 3.11 (minimum supported)
- [x] All 73 tests pass, 100% coverage

# Verification

```bash
python -m pytest --tb=short -q --cov=cli_replay --cov-fail-under=100
python -c "from cli_replay import __version__; print(__version__)"
```