---
id: T4.4
title: CLI redact subcommand + validation + tests
plan: plan-2026-03-auto-redact
type: feature
priority: P1
complexity: 15
status: done
sprint: sprint-4
tags: []
depends_on: []
completed_at: '2026-03-11T13:13:28.369871'
---

# Objective

Add `clirec redact` subcommand to `cli.py` — wires up the redact module with CLI arg parsing and output file handling.

# Implementation Plan

- Add `redact_parser` to cli.py subparsers: `file` (positional), `-o/--output` (optional)
- Add dispatch case in `_run()` for `args.command == "redact"`
- If `-o` provided, write to file; otherwise write to stdout
- Follow reflow subcommand pattern exactly

# Acceptance Criteria

- [x] `clirec redact file.clirec` writes redacted output to stdout
- [x] `clirec redact file.clirec -o clean.clirec` writes to file
- [x] Missing file produces clean error exit
- [x] `clirec redact --help` shows correct usage
- [x] Tests cover: default args, output flag, missing file error, help text

# Verification

- `pytest tests/test_cli.py::TestRedactArgParsing -v`
- `bpsai-pair arch check cli_replay/cli.py`