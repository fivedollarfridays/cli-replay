---
id: T1.5
title: CLI integration tests
plan: plan-2026-03-clireplay-v1
type: feature
priority: P2
complexity: 30
status: done
sprint: null
tags: []
depends_on:
- T1.3
- T1.4
completed_at: '2026-03-04T02:34:13.802422'
---

# Objective

Verify the `clirec` CLI entry point works end-to-end. No new source code — `cli.py` is already complete. This task adds integration tests confirming the argparse wiring and error handling.

# Implementation Plan

1. Test `play` subcommand argument parsing (defaults + all flags)
2. Test `record` subcommand argument parsing (`-o` flag)
3. Test no-subcommand shows help
4. End-to-end: `play` fixture file with mocked sleep, verify stdout output
5. Error case: nonexistent file produces error

# Acceptance Criteria

- [x] `clirec play file.clirec` parses with correct defaults (speed=1.0, max_delay=3.0)
- [x] `clirec play --speed 2 --max-delay 5 --no-input --instant file.clirec` parses all flags
- [x] `clirec record -o demo` parses output name
- [x] `clirec` with no args triggers help
- [x] End-to-end play of fixture file produces expected stdout
- [x] All tests pass with `pytest tests/test_cli.py`

# Verification

- `pytest tests/test_cli.py -v` — all tests pass
- `pytest tests/ -v` — full test suite passes
- `pip install -e .` and `clirec --help` works