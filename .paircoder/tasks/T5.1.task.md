---
id: T5.1
title: Fix demo-planned git tag to pre-implementation state
plan: plan-2026-03-demo-reset
type: chore
priority: P0
complexity: 10
status: done
sprint: 5
tags:
- test-store
- demo
- git
depends_on: []
completed_at: '2026-03-17T17:43:20.212798'
---

# Objective

Move the `demo-planned` tag in `~/test-store` back to a pre-implementation commit (`0fb5ac7` base) with the corrected `video3-session1.txt` ("See ya!" triggers) applied on top.

Currently `demo-planned` points to HEAD which contains the T1.1 search endpoint implementation. Running `video3-reset.sh` silently resets to a dirty state.

# Implementation Plan

1. In `~/test-store`, run `git checkout 0fb5ac7 -- video3-session1.txt` to restore the old script
2. Apply the 4 "See ya!" trigger fixes to `video3-session1.txt` (replace all `@wait-for! "go: command not found" 600` + MANUAL comment blocks with `@wait-for "See ya!" 600` + `@wait-for! "test-store$" 5`)
3. `git add video3-session1.txt && git commit -m "fix: See ya! triggers for demo recording"`
4. `git tag -f demo-planned HEAD`

# Acceptance Criteria

- [x] `git show demo-planned -- tests/test_search.py` returns empty (no test file at tag)
- [x] `git show demo-planned -- src/main.py` shows ONLY the `/products` endpoint, no search
- [x] `video3-session1.txt` at `demo-planned` contains `@wait-for "See ya!" 600` (4 instances)
- [x] `git reset --hard demo-planned` in test-store leaves working tree clean with no implementation code

# Verification

```bash
cd ~/test-store
git show demo-planned -- tests/test_search.py   # should be empty
git show demo-planned:src/main.py | grep search  # should return nothing
git show demo-planned:video3-session1.txt | grep "See ya"  # should show 4 matches
git reset --hard demo-planned && git status  # should show clean tree
```